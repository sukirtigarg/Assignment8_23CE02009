#include <stdio.h>
#include <string.h>
int main()
{
    int n1, n2, str1[50], str2[50];
    printf("Enter the length of first string: ");
    scanf("%d", &n1);
    printf("Enter the first string: ");
    gets(str1);
    printf("Enter the length of second string: ");
    scanf("%d", &n2);
    printf("Enter the second string: ");
    gets(str2);
    

}